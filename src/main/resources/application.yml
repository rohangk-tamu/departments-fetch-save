server:
    port: 8090


spring:
    application:
        name: Spring-boot-tutorial
    datasource:
        driver-class-name: com.mysql.jdbc.Driver
        password: root
        url: jdbc:mysql://localhost:3306/departmentsDb
        username: root
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true

management:
    endpoints:
        web:
            exposure:
                include: "*"
    info:
        java:
            enabled: true
